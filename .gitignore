# binaries
bin/

# build artifacts
*.exe
*.test
*.out

# agent runtime state (Phase 4 buffer)
*.jsonl
*.db

# local config (contains secrets — use agent.yaml.example as template)
agent.yaml

# editor / OS
.DS_Store
.idea/
.vscode/
